seml:
  executable: scripts/run_experiment.py
  conda_environment: your_env
  project_root_dir: /nfs/homedirs/daro/quantization-reliability
  output_dir: /nfs/students/daro/results

slurm:
  partition: gpu_large
  gres: gpu:1
  cpus-per-task: 4
  mem: 16G
  time: '02:00:00'
  experiments_per_job: 1

grid:
  model_name:
    choice:
      - 'huggingface/llama-2'

fixed:
  batch_size: 8
  dataset: 'dummy'
