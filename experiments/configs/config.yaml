defaults:
  - hardware: single_gpu
  - dataset: triviaqa
  - _self_

# Must be overridden per run
exp_id: ???
model_name: ???

# Tunable per run
num_entries: 1000
batch_size: auto
max_new_tokens: 15
temperature: 0.7

# Set to false to run without W&B logging (useful for debugging)
use_wandb: true
